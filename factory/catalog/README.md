# Catalog Module (Spec 000)

This module implements the functionality described in `docs/specs/000-*.md`.

## Quick Start

Run this module in mock mode:
```bash
python -m factory.catalog --mock-mode
```

## Typical Usage

See `tests/test_catalog_typical_usage.py` for a complete integration example.
