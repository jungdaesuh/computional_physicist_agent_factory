# Literature Module (Spec 007)

This module implements the OpenAlex literature-discovery surface described in
`docs/specs/007-literature-discovery.md`.

## Quick Start

Run this module in mock mode:
```bash
python -m factory.literature --mock-mode mine-gaps --seed-query "QI stellarator coil simplicity"
```

## Typical Usage

See `tests/test_literature_typical_usage.py` for a complete integration example.
