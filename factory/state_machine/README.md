# State_machine Module (Spec 000)

This module implements the functionality described in `docs/specs/000-*.md`.

## Quick Start

Run this module in mock mode:
```bash
python -m factory.state_machine --mock-mode
```

## Typical Usage

See `tests/test_state_machine_typical_usage.py` for a complete integration example.
